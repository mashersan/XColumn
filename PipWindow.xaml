<Window x:Class="XColumn.PipWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="PiP Mode" Height="600" Width="800"
        WindowStyle="None" Topmost="True" AllowsTransparency="True" Background="Transparent"
        ResizeMode="CanResize">

    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="8" CaptionHeight="0" CornerRadius="0" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Border CornerRadius="8" Background="#15202B" BorderBrush="#38444D" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="24"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#22303C" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock Text="XColumn PiP" Foreground="LightGray" FontSize="11" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <Button Content="✕" Width="30" HorizontalAlignment="Right" Background="Transparent" Foreground="LightGray" BorderThickness="0" Click="Close_Click"/>
            </Grid>

            <wv2:WebView2 x:Name="PipWebView" Grid.Row="1" />
        </Grid>
    </Border>
</Window>