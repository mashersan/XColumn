<Window x:Class="XColumn.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:p="clr-namespace:XColumn.Properties"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.Settings_Title}" Height="600" Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource DefaultWindowStyle}"
        Background="{DynamicResource SystemControlPageBackgroundChromeLowBrush}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" Margin="0,0,0,12">
            <TabItem Header="{x:Static p:Resources.Settings_Tab_Display}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,12,0">
                    <StackPanel Margin="10">
                        <TextBlock Text="{x:Static p:Resources.Settings_Language}" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                        <ComboBox Name="LanguageComboBox" Margin="10,0,0,5" HorizontalAlignment="Left" Width="200">
                            <ComboBoxItem Content="日本語 (Japanese)" Tag="ja-JP"/>
                            <ComboBoxItem Content="English" Tag="en-US"/>
                        </ComboBox>
                        <TextBlock Text="{x:Static p:Resources.Msg_RestartRequired}" FontSize="11" Foreground="Gray" Margin="10,0,0,20"/>

                        <TextBlock Text="{x:Static p:Resources.Settings_Theme}" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                        <ComboBox Name="ThemeComboBox" Margin="10,0,0,20" HorizontalAlignment="Left" Width="200" SelectedValuePath="Tag">
                            <ComboBoxItem Content="System" Tag="System"/>
                            <ComboBoxItem Content="Light" Tag="Light"/>
                            <ComboBoxItem Content="Dark" Tag="Dark"/>
                        </ComboBox>

                        <TextBlock Text="{x:Static p:Resources.Settings_Font}" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                        <Grid Margin="10,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Font:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <ComboBox Grid.Column="1" Name="FontFamilyComboBox" IsEditable="True" Margin="0,0,15,0" ToolTip="{x:Static p:Resources.Tooltip_FontFamily}"/>
                            <TextBlock Grid.Column="2" Text="Size(px):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Grid.Column="3" Name="FontSizeTextBox" Width="50" HorizontalAlignment="Left" VerticalAlignment="Center" VerticalContentAlignment="Center" ToolTip="{x:Static p:Resources.Tooltip_FontSize}"/>
                        </Grid>

                        <TextBlock Text="{x:Static p:Resources.Settings_DisplayOptions}" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                        <StackPanel Margin="10,0,0,0">
                            <CheckBox Name="HideMenuHomeCheckBox" Content="{x:Static p:Resources.Settings_HideMenuHome}" Margin="0,0,0,8"/>
                            <CheckBox Name="HideMenuNonHomeCheckBox" Content="{x:Static p:Resources.Settings_HideMenuNonHome}" Margin="0,0,0,8"/>
                            <CheckBox Name="HideListHeaderCheckBox" Content="{x:Static p:Resources.Settings_HideListHeader}" Margin="0,0,0,8"/>
                            <CheckBox Name="HideRightSidebarCheckBox" Content="{x:Static p:Resources.Settings_HideRightSidebar}" Margin="0,0,0,8"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="{x:Static p:Resources.Settings_Tab_Column}">
                <StackPanel Margin="10">
                    <TextBlock Text="{x:Static p:Resources.Settings_Layout}" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                    <CheckBox Name="UseUniformGridCheckBox" Content="{x:Static p:Resources.Settings_UniformGrid}" Margin="10,0,0,8" Click="UseUniformGridCheckBox_Click" ToolTip="{x:Static p:Resources.Tooltip_UniformGrid}"/>
                    <CheckBox Name="ShowColumnUrlCheckBox" Content="{x:Static p:Resources.Settings_ShowColumnUrl}" Margin="10,0,0,8"/>

                    <TextBlock Text="{x:Static p:Resources.Settings_ColumnWidth}" Margin="10,10,0,5"/>
                    <Grid Margin="10,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Slider Name="ColumnWidthSlider" Grid.Column="0" Minimum="300" Maximum="1024" Value="380" TickFrequency="10" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding ElementName=ColumnWidthSlider, Path=Value}" VerticalAlignment="Center" Margin="10,0,0,0" Width="30" TextAlignment="Right"/>
                    </Grid>

                    <Separator Margin="0,0,0,15"/>

                    <TextBlock Text="{x:Static p:Resources.Settings_NewColumnPos}" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                    <CheckBox Name="AddColumnToLeftCheckBox" Content="{x:Static p:Resources.Settings_AddLeft}" Margin="10,0,0,5" ToolTip="{x:Static p:Resources.Tooltip_AddColumnLeft}"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="{x:Static p:Resources.Settings_Tab_Behavior}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <TextBlock Text="{x:Static p:Resources.Settings_BasicBehavior}" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                        <CheckBox Name="UseSoftRefreshCheckBox" Content="{x:Static p:Resources.Settings_SoftRefresh}" Margin="10,0,0,8" ToolTip="{x:Static p:Resources.Tooltip_SoftRefresh}"/>
                        <CheckBox Name="KeepUnreadPositionCheckBox" Content="{x:Static p:Resources.Settings_KeepUnread}" Margin="10,0,0,8" ToolTip="{x:Static p:Resources.Tooltip_KeepUnread}"/>
                        <CheckBox Name="EnableWindowSnapCheckBox" Content="{x:Static p:Resources.Settings_WindowSnap}" Margin="10,0,0,5"/>
                        <TextBlock Text="*Press Ctrl to disable temporarily" Margin="28,0,0,20" Foreground="Gray" FontSize="11"/>

                        <TextBlock Text="{x:Static p:Resources.Settings_FocusMode}" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                        <CheckBox Name="DisableFocusModeOnMediaClickCheckBox" Content="{x:Static p:Resources.Settings_DisableFocusMedia}" Margin="10,0,0,8"/>
                        <CheckBox Name="DisableFocusModeOnTweetClickCheckBox" Content="{x:Static p:Resources.Settings_DisableFocusTweet}" Margin="10,0,0,20"/>

                        <TextBlock Text="{x:Static p:Resources.Settings_ListAutoNavDelay}" Margin="10,10,0,5"/>
                        <TextBox Name="ListAutoNavDelayTextBox" Width="100" HorizontalAlignment="Left" Margin="10,0,0,20"  ToolTip="{x:Static p:Resources.Tooltip_ListAutoNavDelay}"/>
                        <TextBlock Text="{x:Static p:Resources.Settings_ListAutoNavDelayWarning}" FontSize="11" Foreground="Gray" Margin="12,0,0,20"/>

                        <TextBlock Text="{x:Static p:Resources.Settings_ServerMonitor}" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                        <Grid Margin="10,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="0">
                                <TextBlock Text="{x:Static p:Resources.Settings_CheckInterval}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <ComboBox Name="ServerCheckIntervalComboBox" Width="120" VerticalContentAlignment="Center">
                                    <ComboBoxItem Content="1 min" Tag="1"/>
                                    <ComboBoxItem Content="3 min" Tag="3"/>
                                    <ComboBoxItem Content="5 min" Tag="5"/>
                                    <ComboBoxItem Content="10 min" Tag="10"/>
                                    <ComboBoxItem Content="15 min" Tag="15"/>
                                    <ComboBoxItem Content="30 min" Tag="30"/>
                                    <ComboBoxItem Content="60 min" Tag="60"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="{x:Static p:Resources.Settings_Tab_Mute}">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="{x:Static p:Resources.Settings_NgWordTitle}" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                    <TextBlock Text="{x:Static p:Resources.Settings_NgWordDesc}" HorizontalAlignment="Right" FontSize="12" Margin="0,0,0,10" Foreground="Gray"/>

                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Name="NgWordInputBox" ui:ControlHelper.PlaceholderText="{x:Static p:Resources.Settings_NgWordPlaceholder}" Margin="0,0,10,0"/>
                        <Button Content="{x:Static p:Resources.Common_Add}" Click="AddNgWordButton_Click" Grid.Column="1"/>
                    </Grid>

                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ListBox Name="NgWordListBox" Margin="0,0,0,10" SelectionMode="Extended"/>
                        <Button Grid.Row="1" Content="{x:Static p:Resources.Common_DeleteSelected}" Click="DeleteNgWordButton_Click" HorizontalAlignment="Right"/>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="{x:Static p:Resources.Settings_Tab_CSS}">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{x:Static p:Resources.Settings_CssLabel}" Margin="0,0,0,8" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"/>
                    <TextBox Grid.Row="1" Name="CustomCssTextBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas, Courier New, Monospace" Padding="5"/>
                </Grid>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static p:Resources.Common_OK}" Width="100" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="{x:Static p:Resources.Common_Cancel}" Width="110" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>