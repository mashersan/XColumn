<Window x:Class="XColumn.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XColumn"
        mc:Ignorable="d"
        Title="設定" Height="320" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="表示オプション" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="左側メニューの非表示設定:" FontWeight="Bold" Margin="0,0,0,5" Foreground="#555"/>

            <CheckBox Name="HideMenuHomeCheckBox" Content="ホームでメニューを非表示にする" 
                      Margin="10,0,0,5" ToolTip="ホーム(https://x.com/home)を表示しているカラムでメニューバーを隠します。"/>

            <CheckBox Name="HideMenuNonHomeCheckBox" Content="ホーム以外でメニューを非表示にする" 
                      Margin="10,0,0,15" ToolTip="通知・検索・リストなど、ホーム以外のカラムでメニューバーを隠します。"/>

            <Separator Margin="0,5,0,10" />

            <TextBlock Text="リスト表示の調整:" FontWeight="Bold" Margin="0,0,0,5" Foreground="#555"/>
            <CheckBox Name="HideListHeaderCheckBox" Content="リストのヘッダーを簡略化する" 
                      Margin="10,0,0,10" ToolTip="リスト表示時の画像・詳細情報・戻るボタンを隠し、タイムラインを広く表示します。"/>

            <Separator Margin="0,10" />
            <TextBlock Text="※設定変更は即時反映されますが、&#x0a;表示が崩れる場合はリロードしてください。" Foreground="Gray" FontSize="11" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Width="80" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="キャンセル" Width="80" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>